apiVersion: v1
kind: Secret
metadata:
  name: finance-secrets
type: Opaque
stringData:
  # Injected into the container as environment variables at runtime.
  # stringData lets us write plain values; Kubernetes stores them base64-encoded.
  FLASK_SECRET_KEY: "dfe6ef10d55fbbb5a207a7844d63bb0600e2f53d1ba5db39b74c3d73c1531942"
  # No DATABASE_URL here yet, so the app falls back to its built-in SQLite path.
  # We'll add Postgres later when we move toward EKS.